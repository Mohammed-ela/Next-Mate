rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 🚧 RÈGLES TEMPORAIRES POUR LE DÉVELOPPEMENT
    // ⚠️ À REMPLACER EN PRODUCTION
    
    // Permettre tout pour les utilisateurs authentifiés (temporaire)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
    
    // 📝 RÈGLES DE PRODUCTION (à décommenter plus tard) :
    /*
    // Règles pour les utilisateurs
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    
    // Règles pour les conversations
    match /conversations/{conversationId} {
      allow read, write: if request.auth != null && 
        request.auth.uid in resource.data.participants;
      allow create: if request.auth != null && 
        request.auth.uid in request.resource.data.participants;
      
      // Règles pour les messages
      match /messages/{messageId} {
        allow read: if request.auth != null && 
          request.auth.uid in get(/databases/$(database)/documents/conversations/$(conversationId)).data.participants;
        allow create: if request.auth != null && 
          request.auth.uid == request.resource.data.senderId &&
          request.auth.uid in get(/databases/$(database)/documents/conversations/$(conversationId)).data.participants;
        allow update, delete: if false;
      }
    }
    */
  }
} 