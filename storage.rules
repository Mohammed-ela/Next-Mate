rules_version = '2';

// 🔒 Règles Firebase Storage pour NextMate
service firebase.storage {
  match /b/{bucket}/o {
    // 📸 Règles pour les images utilisateurs
    match /users/{userId}/{allPaths=**} {
      // ✅ Permettre lecture à tous (pour voir les avatars)
      allow read: if true;
      
      // ✅ Permettre écriture seulement au propriétaire authentifié
      allow write: if request.auth != null 
                   && request.auth.uid == userId
                   && resource == null || resource.size < 5 * 1024 * 1024; // Max 5MB
    }
    
    // 🚫 Bloquer tout le reste
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
} 